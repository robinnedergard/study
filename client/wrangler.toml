name = "study"
compatibility_date = "2024-12-01"
main = "worker/index.ts"

[assets]
directory = "./dist"
binding = "ASSETS"
not_found_handling = "single-page-application"

# Set these as secrets via: npx wrangler secret put PASSCODE && npx wrangler secret put JWT_SECRET
